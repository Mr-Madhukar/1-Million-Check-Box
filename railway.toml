# Railway deployment configuration
# Railway will automatically detect docker-compose.yml and deploy all services

[deploy]
# Number of replicas for the main app
numReplicas = 1
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[healthcheck]
# Railway will poll /health to confirm the app is up
path = "/health"
timeoutSeconds = 30
